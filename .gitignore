# Secrets - NEVER commit these
.dev.vars
.dev.vars.*
.env
.env.*
*.local
# ...but DO ship the example (it holds no real values)
!.dev.vars.example

# Cloudflare / Wrangler
.wrangler/
.mf/
dist/

# DC client (cloned locally for inspection, not part of this repo)
dc/

# Node
node_modules/
npm-debug.log*
yarn-error.log

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
